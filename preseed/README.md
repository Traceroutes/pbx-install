This file should automate a minimal install of Debian 12 from any preferred install media.  It is primarily tested with netinst.

If being netbooted, the kernel should be loaded with the parameter ```interface=auto```
